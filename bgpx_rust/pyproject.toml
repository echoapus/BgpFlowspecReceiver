[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "bgpx_rust"
version = "0.1.0"
requires-python = ">=3.11"
description = "Rust-accelerated BGP parser for bgpx (optional)"

[tool.maturin]
features = ["pyo3/extension-module"]
